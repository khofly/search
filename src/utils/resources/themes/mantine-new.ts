import { MantineThemeOverride } from "@mantine/core";

export const THEME_MANTINE_NEW: MantineThemeOverride = {
  /** Put your mantine theme override here */
  fontFamily: `var(--next-inter), sans-serif`,
  cursorType: "pointer",
};
